/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TrimArrayPipe } from './trim-array.pipe';

describe('Pipe: Trime', () => {
  it('create an instance', () => {
    let pipe = new TrimArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
